{
  "private": true,
  "scripts": {
    "clean": "rimraf packages/**/lib",
    "lint": "tslint -c tslint.json packages/*/src/**/*.ts",
    "test": "jest",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/jest": "^23.3.5",
    "jest": "^23.6.0",
    "jest-spec-reporter": "^1.0.5",
    "lerna": "^3.4.3",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "typescript": "^3.1.1"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
